/*
 * TABOK multiplayer network configuration.
 *
 * After deploying the Worker in turn-relay/, paste its HTTPS URL below.
 * Never put a TURN key or API token in this public file.
 */
window.TABOK_NETWORK = Object.freeze({
  turnCredentialEndpoint: ''
});
